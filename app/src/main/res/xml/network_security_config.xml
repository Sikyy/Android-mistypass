<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Production cert pinning: uncomment and replace pins when api.mistyislet.com is deployed.
        Generate pins with: openssl s_client -connect api.mistyislet.com:443 | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | openssl enc -base64
    -->
    <!--
    <domain-config>
        <domain includeSubdomains="true">api.mistyislet.com</domain>
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">REPLACE_WITH_REAL_PRIMARY_PIN=</pin>
            <pin digest="SHA-256">REPLACE_WITH_REAL_BACKUP_PIN=</pin>
        </pin-set>
    </domain-config>
    -->

    <!-- Debug: allow cleartext for local development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.12</domain>
    </domain-config>
</network-security-config>
